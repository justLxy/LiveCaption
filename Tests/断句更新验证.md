# 0.3 断句验证

已通过编译、签名验证、句子/片段回归测试与 20 秒真实 Metal 模型链路测试（零错误、正常保存尾段并退出）。

回归覆盖用户所报投资条件句、so many 后续、has a 理论段落、in the industry 关系从句、孤立 filler、大小写/标点修订、缩写/小数、等待超时和停止提交。

原始会话的 ASR final 顺序回放保留为 segmentation-replay.txt。回放显示：投资/实验室价值观整句合并；theory/positive future 整句合并；conventional wisdom/industry/disagree with 整句合并。该回放无原始音频与完整 partial 时间线，不能等同实时录音复测。

固定合成语音的真实推理报告为 segmentation-smoke-report.json。三小时测试保持取消。
